<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usuario.juegoelementosquimicos" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/atomo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".JuegoElementos"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InfoElementos"
            android:label="@string/title_activity_info_elementos" >
        </activity>
        <activity
            android:name=".MiNavegador"
            android:label="@string/title_activity_mi_navegador" >
        </activity>
    </application>

</manifest>
